const Heading = ({ title }) => {
  return (
    <div className="my-10 text-green">
      <h1 className="heading uppercase tracking-wider font-semibold md:text-2xl relative">{title}</h1>
    </div>
  );
};

export default Heading;
